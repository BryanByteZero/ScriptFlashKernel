# ScriptFlashKernel
ScriptFlashKernelSimpleTotoro
Kernel Zip Layout Totoro A simple script to flash ONLY a kernel with ramdisk (zImage) with edify and bmunlock (that find correct partition and do a dd to flash).
